# testing_release_linking
Just testing how GitHub behaves when linking to specific files in specific commit works when commit is referenced by a tag.

And now we just some edits,commits and tags...

# 1.2 release
